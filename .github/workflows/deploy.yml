name: Deploy to Shared Hosting

on:
  push:
    branches: [ "main" ]

jobs:
  web-deploy:
    name: Deploying
    runs-on: ubuntu-latest
    steps:
      - name: Get latest code
        uses: actions/checkout@v4

      - name: Verify Deployment Secrets
        run: |
          if [ -z "${{ secrets.FTP_SERVER }}" ]; then echo "::error::FTP_SERVER secret is missing!"; exit 1; fi
          if [ -z "${{ secrets.FTP_USERNAME }}" ]; then echo "::error::FTP_USERNAME secret is missing!"; exit 1; fi
          if [ -z "${{ secrets.FTP_PASSWORD }}" ]; then echo "::error::FTP_PASSWORD secret is missing!"; exit 1; fi

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'

      - name: Install Composer Dependencies
        run: composer install --no-ansi --no-dev --no-interaction --no-plugins --no-progress --no-scripts --optimize-autoloader

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build Assets (Vite)
        run: |
          npm install
          npm run build

      - name: Sync Files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          # Ubah 'server-dir' ke folder tujuan di hosting Anda (misal: sinergi-app/)
          server-dir: sinergi-app/
          # Tambahkan 'protocol: ftps' jika hosting Anda mendukung koneksi aman
          protocol: ftp
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            **/node_modules/**
            **/tests/**
            **/.env
            **/storage/*.key
            **/phpunit.xml
            **/vite.config.js
            **/package.json
            **/package-lock.json
            **/composer.json
            **/composer.lock
            **/README.md
            **/CODE_OF_CONDUCT.md
            **/CONTRIBUTING.md
            **/LICENSE
            **/DEPLOY.md
            **/SECURITY.md
            **/GEMINI.md
            **/phpunit.xml
            **/phpunit.xml.dist
            **/.editorconfig
